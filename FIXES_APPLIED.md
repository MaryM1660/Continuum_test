# Исправления видимости кнопок и стилей SideDrawer

**Дата:** 2026-01-23

---

## ✅ Исправленные проблемы

### 1. Кнопки не видны на светлой теме

**Проблема:** Liquid Glass кнопки использовали слишком низкую opacity (0.15 для primary, 0.12 для secondary), что делало их практически невидимыми на белом фоне.

**Исправление:**
- Увеличена opacity для светлой темы: `0.7` для primary, `0.6` для secondary
- На темной теме используются стандартные значения Apple HIG
- Добавлена темная граница на светлой теме: `rgba(0, 0, 0, 0.15)`
- Заменены все захардкоженные значения на динамические переменные

**Файлы:**
- `src/components/LiquidGlassButton.tsx`

### 2. SideDrawer не соответствует гайдлайнам

**Проблема:** 
- Заголовок использовал `variant="h1"` (который маппится на `largeTitle` - 34pt), что слишком большой для меню
- Spacing и borderWidth не соответствовали Apple HIG стандартам

**Исправление:**
- Заголовок изменен на `variant="title2"` (22pt, Apple HIG стандарт для заголовков в навигации)
- Обновлены spacing значения:
  - `paddingHorizontal: 24` (spacing.xl)
  - `paddingVertical: 16` (spacing.base)
- Border width изменен на `StyleSheet.hairlineWidth` (Apple HIG стандарт для разделителей)
- Добавлен `minHeight: 48` для touch targets (Apple HIG минимум)

**Файлы:**
- `src/components/SideDrawer.tsx`

### 3. Поддержка прямых вариантов Apple HIG в Text компоненте

**Проблема:** Text компонент не поддерживал прямые варианты Apple HIG (`largeTitle`, `title1`, etc.)

**Исправление:**
- Добавлена поддержка прямых вариантов Apple HIG в `variantMapping`
- Обновлен TypeScript интерфейс для поддержки новых вариантов

**Файлы:**
- `src/components/typography/Text.tsx`

---

## 📊 Результаты

### Кнопки
- ✅ Видны на светлой теме (opacity 0.7/0.6)
- ✅ Видны на темной теме (стандартные значения Apple HIG)
- ✅ Правильные границы (темные на светлой, светлые на темной)
- ✅ Правильные тени и эффекты

### SideDrawer
- ✅ Правильная типографика (Title 2 для заголовка)
- ✅ Правильные spacing (24px горизонтально, 16px вертикально)
- ✅ Правильные разделители (hairlineWidth)
- ✅ Правильные touch targets (минимум 48px)

---

## 🎨 Визуальные изменения

### Светлая тема
- Кнопки теперь имеют заметный белый полупрозрачный фон
- Темная граница делает кнопки более заметными
- SideDrawer использует правильную типографику

### Темная тема
- Кнопки используют стандартные значения Apple HIG
- Светлая граница на темном фоне
- Все соответствует гайдлайнам

---

## ✅ Готово к проверке

Все исправления применены. Приложение готово к визуальной проверке в браузере.


