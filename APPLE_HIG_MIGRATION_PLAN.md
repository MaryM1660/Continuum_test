# План миграции на Apple HIG стили

## ✅ Что уже сделано

1. **Создана новая структура файлов стилей** в `src/theme/apple-hig/`:
   - `colors.ts` - все цвета из Apple HIG (System Colors, Labels, Backgrounds, Separators, Liquid Glass)
   - `typography.ts` - типографика из Apple HIG (iOS/iPadOS Text Styles)
   - `spacing.ts` - spacing система (8pt grid)
   - `layout.ts` - layout система (контейнеры, сетки, выравнивание)
   - `materials.ts` - материалы (Liquid Glass эффекты)
   - `shadows.ts` - тени (внешние и внутренние для Liquid Glass)
   - `borderRadius.ts` - радиусы скругления
   - `theme.ts` - главный файл темы (объединяет все)
   - `index.ts` - экспорт всех модулей

2. **Все значения строго соответствуют Apple HIG**:
   - Ничего не выдумано
   - Все взято из официальных гайдлайнов
   - Исключены platform-specific элементы (watchOS, tvOS, visionOS, macOS специфичные)

## 📋 Следующие шаги

### Этап 1: Интеграция новой темы в ThemeContext

1. **Обновить `src/theme/ThemeContext.tsx`**:
   - Импортировать `createAppleHIGTheme` из `apple-hig/theme`
   - Заменить `lightTheme` и `darkTheme` на новую систему
   - Обновить типы для совместимости

2. **Проверить совместимость типов**:
   - Убедиться, что `AppleHIGTheme` совместим с текущим `Theme` типом
   - При необходимости создать адаптер или обновить типы

### Этап 2: Обновление компонентов

3. **Обновить компоненты для использования новых стилей**:
   - `src/components/LiquidGlassButton.tsx` - использовать `theme.materials.liquidGlass`
   - `src/components/typography/Text.tsx` - использовать `theme.typography.*`
   - `src/components/layout/*` - использовать `theme.layout.patterns.*`
   - Все остальные компоненты

4. **Удалить захардкоженные стили**:
   - Найти все `StyleSheet.create` с захардкоженными значениями
   - Заменить на значения из `theme.*`
   - Удалить inline стили с захардкоженными значениями

### Этап 3: Удаление старых файлов

5. **Удалить старые файлы стилей** (после полной миграции):
   - `src/theme/colors.ts` (старый)
   - `src/theme/typography.ts` (старый)
   - Вспомогательные файлы (если не нужны):
     - `src/theme/SPACING_GUIDE.md`
     - `src/theme/TYPOGRAPHY_AUDIT.md`
     - `src/theme/TYPOGRAPHY_BROWSER_CHECK.md`
     - `src/theme/TYPOGRAPHY_GUIDE.md`
     - `src/theme/TYPOGRAPHY_RULES.md`

### Этап 4: Финальная проверка

6. **Проверить все экраны**:
   - `src/screens/TalkScreen.tsx`
   - `src/screens/SessionNotesScreen.tsx`
   - `src/screens/CoachNotesScreen.tsx`
   - `src/screens/AccountScreen.tsx`
   - Все остальные экраны

7. **Проверить соответствие Apple HIG**:
   - Все цвета соответствуют System Colors
   - Все размеры шрифтов соответствуют Text Styles
   - Все spacing соответствуют 8pt grid
   - Все радиусы соответствуют стандартам
   - Liquid Glass эффекты соответствуют гайдлайнам

## 📝 Важные замечания

### Что включено из Apple HIG:
- ✅ System Colors (Light/Dark Mode)
- ✅ Label Colors (с правильными opacity)
- ✅ Background Colors
- ✅ Separator Colors
- ✅ Typography (iOS/iPadOS Text Styles)
- ✅ Spacing (8pt grid система)
- ✅ Layout (контейнеры, сетки, выравнивание)
- ✅ Materials (Liquid Glass)
- ✅ Shadows (внешние и внутренние)
- ✅ Border Radius

### Что исключено (platform-specific):
- ❌ watchOS специфичные размеры
- ❌ tvOS специфичные размеры
- ❌ visionOS специфичные размеры
- ❌ macOS специфичные размеры
- ❌ SF Symbols (это библиотека иконок, не стили)
- ❌ Digital Crown, Apple Pencil, Eyes tracking
- ❌ Complications, Watch faces
- ❌ Platform-specific компоненты

## 🔍 Проверка соответствия

После миграции проверить:
1. Все цвета используют `theme.colors.*`
2. Все размеры шрифтов используют `theme.typography.*`
3. Все spacing используют `theme.spacing.*`
4. Все радиусы используют `theme.borderRadius.*`
5. Все тени используют `theme.shadows.*`
6. Все материалы используют `theme.materials.*`
7. Нет захардкоженных значений цветов, размеров, отступов

## 📚 Ссылки на документацию

- [Apple HIG - Color](https://developer.apple.com/design/human-interface-guidelines/color)
- [Apple HIG - Typography](https://developer.apple.com/design/human-interface-guidelines/typography)
- [Apple HIG - Layout](https://developer.apple.com/design/human-interface-guidelines/layout)
- [Apple HIG - Materials](https://developer.apple.com/design/human-interface-guidelines/materials)

