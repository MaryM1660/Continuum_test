# 📱 User Flow - Continuum AI Career Coach

## Общая структура приложения

### Экраны (Screens):
1. **TalkScreen** - главный экран с онбордингом и голосовым взаимодействием
2. **SessionNotesScreen** - заметки пользователя (редактируемые)
3. **CoachNotesScreen** - внутренние заметки коуча (read-only)
4. **AccountScreen** - аккаунт и поддержка

### Компоненты навигации:
- **SideDrawer** - боковое меню (открывается с TalkScreen)

---

## 🎯 ДЕТАЛЬНЫЙ USER FLOW

### 📍 ЭКРАН 1: TalkScreen (Главный экран)

#### Состояния экрана:
- `onboardingStep`: 1 | 2 | 3 | 'complete'
- `isMuted`: true/false (микрофон выключен/включен)
- `isRecording`: true/false (идет запись)
- `isSpeaking`: true/false (коуч говорит)
- `isProcessingLLM`: true/false (обработка LLM)
- `isWaitingForUser`: true/false (ожидание действия пользователя)
- `recognizedText`: string (распознанный текст)
- `hasMicPermission`: true/false

---

### 🚀 ОНБОРДИНГ (Onboarding Steps)

#### **STEP 1: "Hello!"**
**Что видит пользователь:**
- Облако (анимированное, но без звука)
- Текст: "Hello!"
- Кнопка: "Hi"

**Что происходит:**
- При загрузке: `onboardingStep = 1`, `isWaitingForUser = true`
- Облако анимируется (но без звука)
- НЕТ речи коуча (голос не включается)

**Действия пользователя:**
1. Нажимает кнопку "Hi"
   - **Реакция:** 
     - `handleStep1()` вызывается
     - `setIsWaitingForUser(false)` - скрывает кнопку
     - `setOnboardingStep(2)` - переход на шаг 2
     - `setIsWaitingForUser(true)` - показывает кнопку на шаге 2

---

#### **STEP 2: "I am AI career coach..."**
**Что видит пользователь:**
- Облако (анимированное, но без звука)
- Текст: "I am AI career coach. I can help you to define your career strategy, talk to you about your experience and goals, or discuss career-related docs"
- Кнопка: "Ok"

**Что происходит:**
- НЕТ речи коуча (голос не включается)
- Микрофонные кнопки НЕ отображаются

**Действия пользователя:**
1. Нажимает кнопку "Ok"
   - **Реакция:**
     - `handleStep2()` вызывается
     - `setOnboardingStep(3)` - переход на шаг 3
     - `setIsWaitingForUser(true)` - показывает кнопку на шаге 3

---

#### **STEP 3: "It would be great if you start talking to me by voice. Is that ok?"**
**Что видит пользователь:**
- Облако (анимированное, но без звука)
- Текст: "It would be great if you start talking to me by voice. Is that ok?"
- Одна кнопка: "Turn on the mic"

**Что происходит:**
- НЕТ речи коуча (голос не включается)
- Этот экран показывается:
  - Во время онбординга (после STEP 2)
  - При загрузке TalkScreen, если разрешения на микрофон нет
  - При попытке включить микрофон на главном экране, если разрешения нет

**Действия пользователя:**

1. **Нажимает кнопку "Turn on the mic"**
   - **Реакция:**
     - `handleStep3()` вызывается
     - Запрашивается разрешение через `requestMicPermission()`
     - **БЕЗ разрешения НЕ переходим дальше:**
       - Показывается Alert с инструкциями
       - Остаемся на STEP 3
       - Кнопка снова становится доступной для повторной попытки
     - **ТОЛЬКО если разрешение получено:**
       - `setHasMicPermission(true)`
       - `setOnboardingStep('complete')` - переход на главный экран
       - `setIsMuted(false)`
       - `llmService.resetConversation()` - инициализация LLM
       - `handleMainScreenWelcome()` вызывается (показывает текст и озвучивает приветствие)

---

### 🎤 ГЛАВНЫЙ ЭКРАН (onboardingStep === 'complete')

**Что видит пользователь:**
- Кнопка меню (иконка "Bars3") в левом верхнем углу
- Облако (анимированное, реагирует на звук и речь)
- Контент внизу экрана:
  - Статус "Thinking..." (если обрабатывается LLM)
  - Текст приветствия (отображается СРАЗУ, до озвучки): 
    - "I'm here to help you think through your career direction..."
    - "What would you like to start with?"
  - Инструкция "Listening... Speak now" (если идет запись)
  - Две кнопки опций (всегда активны, независимо от записи):
    - "Follow the coach's plan"
    - "Discuss your topic or document"
  - Микрофонные кнопки (всегда видны)

**Что происходит:**
- После перехода с шага 3:
  - `handleMainScreenWelcome()` вызывается
  - `setDisplayText()` устанавливает текст для отображения СРАЗУ
  - `setSpeechText()` устанавливает текст для озвучки
  - Текст отображается на экране ДО начала озвучки
  - Озвучивание происходит асинхронно (не блокирует UI)
  - `setIsWaitingForUser(true)` - показываются кнопки опций

**Действия пользователя:**

#### 1. **Нажимает кнопку меню (Bars3)**
   - **Реакция:**
     - `onOpenDrawer()` вызывается
     - Открывается `SideDrawer` (слева)

#### 2. **Нажимает левую кнопку микрофона (выбор устройства)**
   - **Реакция:**
     - `handleMicSelect()` вызывается
     - На веб: Alert с инструкциями
     - На мобильных: открывается `DeviceSelector`

#### 3. **Нажимает правую кнопку микрофона (громкость)**
   - **Реакция:**
     - `handleSoundLevel()` вызывается
     - Открывается `VolumeSlider`
     - Пользователь меняет громкость → `handleVolumeChange()` → `setVolume(newVolume)`

#### 4. **Нажимает центральную кнопку микрофона (включение/выключение)**
   
   **Если микрофон ВЫКЛЮЧЕН (`isMuted === true`):**
   - **Реакция:**
     - `handleToggleMute()` вызывается
     - Если нет разрешения (`!hasMicPermission`):
       - Переход на STEP 3 для запроса разрешения
       - `setOnboardingStep(3)`
       - `setIsWaitingForUser(true)`
     - Если разрешение есть:
       - Запускается `microphoneService.startRecording()` (для визуализации)
       - Запускается `voiceRecognitionService.startListening()` (для распознавания)
       - `setIsRecording(true)`
       - `setIsMuted(false)`
       - Показывается "Listening... Speak now"
       - Облако реагирует на уровень звука
       - Распознанный текст сохраняется внутренне (НЕ показывается на экране)
   
   **Если микрофон ВКЛЮЧЕН (`isMuted === false`):**
   - **Реакция:**
     - `handleToggleMute()` вызывается
     - Останавливается `microphoneService.stopRecording()`
     - Останавливается `voiceRecognitionService.stopListening()`
     - `setIsRecording(false)`
     - `setIsMuted(true)`
     - `setRecognizedText('')` - очищается внутренний текст

#### 5. **Говорит в микрофон (когда `isRecording === true`)**
   - **Реакция:**
     - `voiceRecognitionService` распознает речь в реальном времени
     - Промежуточные результаты → `setRecognizedText(result.text)` (обновляется внутренне, НЕ показывается на экране)
     - Финальные результаты → через 500ms вызывается `handleUserSpeech(result.text)`
     - Распознанный текст записывается на бэк через `logToBackend('voice', text)`
     - Облако анимируется в зависимости от уровня звука

#### 6. **Завершает речь (пауза 2-3 секунды)**
   - **Реакция:**
     - Если финальный результат получен → `handleUserSpeech(text)` вызывается
     - Если финальный результат не получен → `onSilenceCallback` срабатывает через 2 секунды → `handleUserSpeech(finalText)`
     - Останавливается запись (`microphoneService.stopRecording()`, `voiceRecognitionService.stopListening()`)
     - `setIsRecording(false)`
     - `setIsProcessingLLM(true)` - показывается "Thinking..."
     - Текст отправляется в `llmService.chat(text)`
     - После получения ответа:
       - `setIsProcessingLLM(false)`
       - Вызывается `speak(response.text)` - озвучивается ответ
       - `setIsSpeaking(true)` во время речи
       - Облако анимируется во время речи
       - После завершения речи:
         - `setIsSpeaking(false)`
         - Автоматически возобновляется запись (если микрофон был включен)
         - `setIsRecording(true)`
         - Цикл повторяется

#### 7. **Нажимает кнопку "Follow the coach's plan"**
   - **Реакция:**
     - `handleOption1()` вызывается
     - Выбор записывается на бэк через `logToBackend('button', "Follow the coach's plan")`
     - `setSpeechText()` устанавливает текст для озвучки
     - Озвучивается фраза (через `speak()`)
     - Кнопки остаются активными (не скрываются)

#### 8. **Нажимает кнопку "Discuss your topic or document"**
   - **Реакция:**
     - `handleOption2()` вызывается
     - Выбор записывается на бэк через `logToBackend('button', "Discuss your topic or document")`
     - `setSpeechText()` устанавливает текст для озвучки
     - Озвучивается фраза (через `speak()`)
     - Кнопки остаются активными (не скрываются)

---

### 📝 ЭКРАН 2: SessionNotesScreen

**Что видит пользователь:**
- Кнопка "Назад" (стрелка влево)
- Заголовок "Session Notes"
- Поле поиска "Search notes..."
- Хештеги-фильтры (горизонтальный скролл):
  - #career, #goals, #skills, #challenges, #insights
- Большое текстовое поле для редактирования заметок

**Действия пользователя:**

1. **Нажимает кнопку "Назад"**
   - **Реакция:** `navigation.goBack()` - возврат на TalkScreen

2. **Вводит текст в поле поиска**
   - **Реакция:** `setSearchQuery(text)` - обновляется поисковый запрос (пока не реализована фильтрация)

3. **Нажимает на хештег**
   - **Реакция:** `toggleHashtag(hashtag)` - добавляет/убирает хештег из фильтров (пока не реализована фильтрация)

4. **Вводит текст в поле заметок**
   - **Реакция:** `setNotes(text)` - сохраняется текст заметок (пока только в state, не сохраняется)

---

### 📋 ЭКРАН 3: CoachNotesScreen

**Что видит пользователь:**
- Кнопка "Назад" (стрелка влево)
- Заголовок "Coach's Internal Notes"
- Подзаголовок "Read-only"
- Список карточек с заметками коуча:
  - Заголовок
  - Дата
  - Содержание

**Действия пользователя:**

1. **Нажимает кнопку "Назад"**
   - **Реакция:** `navigation.goBack()` - возврат на TalkScreen

2. **Прокручивает список заметок**
   - **Реакция:** ScrollView прокручивается (только просмотр, редактирование невозможно)

---

### ⚙️ ЭКРАН 4: AccountScreen

**Что видит пользователь:**
- Кнопка "Назад" (стрелка влево)
- Заголовок "Account & Support"
- Секция "Account":
  - Profile
  - Subscription
  - Settings
- Секция "Support":
  - Help Center
  - Contact Support
  - Send Feedback
- Секция "Legal":
  - Privacy Policy
  - Terms of Service

**Действия пользователя:**

1. **Нажимает кнопку "Назад"**
   - **Реакция:** `navigation.goBack()` - возврат на TalkScreen

2. **Нажимает на любой пункт меню**
   - **Реакция:** Пока ничего не происходит (не реализовано)

---

### 🍔 SideDrawer (Боковое меню)

**Когда открывается:**
- Только на главном экране (TalkScreen, `onboardingStep === 'complete'`)
- При нажатии на кнопку меню (Bars3)

**Что видит пользователь:**
- Заголовок "Menu"
- Кнопка закрытия (X)
- Пункты меню:
  - Session Notes
  - Coach's Internal Notes
  - Account & Support
- Переключатель темы (Light/Dark) внизу

**Действия пользователя:**

1. **Нажимает кнопку закрытия (X) или на затемненную область**
   - **Реакция:** `onClose()` - закрывается drawer

2. **Нажимает "Session Notes"**
   - **Реакция:** 
     - `navigation.navigate('SessionNotes')` - переход на SessionNotesScreen
     - Drawer закрывается

3. **Нажимает "Coach's Internal Notes"**
   - **Реакция:**
     - `navigation.navigate('CoachNotes')` - переход на CoachNotesScreen
     - Drawer закрывается

4. **Нажимает "Account & Support"**
   - **Реакция:**
     - `navigation.navigate('Account')` - переход на AccountScreen
     - Drawer закрывается

5. **Переключает тему (Light/Dark)**
   - **Реакция:** `toggleTheme()` - меняется тема приложения

---

## 🔄 ПОЛНЫЙ ЦИКЛ ГОЛОСОВОГО ВЗАИМОДЕЙСТВИЯ

### Идеальный flow после включения микрофона:

1. **Пользователь включает микрофон** → `isMuted = false`, `isRecording = true`
2. **Пользователь говорит** → текст распознается в реальном времени, отображается
3. **Пользователь заканчивает речь (пауза)** → финальный текст отправляется в LLM
4. **LLM обрабатывает** → показывается "Thinking..."
5. **LLM отвечает** → ответ озвучивается, облако анимируется
6. **После ответа** → запись автоматически возобновляется
7. **Цикл повторяется** (шаги 2-6)

---

## ✅ ИСПРАВЛЕНИЯ В FLOW

1. **STEP 2:**
   - ✅ Убраны микрофонные кнопки, осталась только кнопка "Ok"

2. **STEP 3:**
   - ✅ Одна кнопка "Turn on the mic"
   - ✅ Без разрешения НЕ переходим дальше
   - ✅ Показывается при загрузке TalkScreen, если разрешения нет
   - ✅ Показывается при попытке включить микрофон без разрешения

3. **Главный экран:**
   - ✅ Распознанный текст НЕ показывается на экране, только записывается на бэк
   - ✅ Текст приветствия отображается СРАЗУ, до начала озвучки
   - ✅ Разделены `displayText` (для отображения) и `speechText` (для озвучки)
   - ✅ Кнопки опций всегда активны, независимо от записи
   - ✅ Выбор кнопки записывается на бэк через `logToBackend('button', text)`

4. **Проверка разрешения:**
   - ✅ При загрузке TalkScreen проверяется разрешение
   - ✅ Если разрешения нет → показывается STEP 3
   - ✅ При попытке включить микрофон без разрешения → переход на STEP 3

---

## ✅ ЧТО ДОЛЖНО БЫТЬ (Правильный flow по MDN):

1. **Speech Recognition сам запрашивает микрофон** при первом `start()`
2. **Не нужен отдельный MicrophoneService** для Speech Recognition
3. **Четкий флаг `isListening`** контролирует весь цикл
4. **Перезапуск только если нужно продолжать** (проверка флага перед перезапуском в `onend`)
5. **Накопление текста до паузы** перед отправкой в LLM

