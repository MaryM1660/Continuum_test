# 📦 Установка Node.js

Node.js не установлен на вашем компьютере. Нужно установить его для работы с React Native и Expo.

## 🚀 Быстрая установка

### Вариант 1: Официальный сайт (рекомендуется)

1. **Перейдите на сайт**: https://nodejs.org/
2. **Скачайте LTS версию** (Long Term Support) - это стабильная версия
3. **Запустите установщик** и следуйте инструкциям
4. **Важно**: При установке убедитесь, что выбрана опция "Add to PATH"
5. **Перезапустите PowerShell/терминал** после установки

### Вариант 2: Через Chocolatey (если установлен)

Если у вас установлен Chocolatey, выполните:
```powershell
choco install nodejs-lts
```

### Вариант 3: Через winget (Windows 10/11)

```powershell
winget install OpenJS.NodeJS.LTS
```

## ✅ Проверка установки

После установки **обязательно закройте и снова откройте PowerShell**, затем выполните:

```powershell
node --version
npm --version
```

Вы должны увидеть версии, например:
```
v20.10.0
10.2.3
```

## 🔄 После установки

1. **Закройте текущий терминал/PowerShell**
2. **Откройте новый терминал**
3. **Перейдите в папку проекта**:
   ```powershell
   cd C:\Users\MM\Desktop\Continuum\UI
   ```
4. **Установите зависимости**:
   ```powershell
   npm install
   ```
5. **Запустите приложение**:
   ```powershell
   npm start
   ```

## ❓ Проблемы?

Если после установки команды `node` и `npm` все еще не работают:

1. **Перезагрузите компьютер** (иногда требуется для обновления PATH)
2. **Проверьте PATH вручную**:
   - Откройте "Переменные среды" в Windows
   - Убедитесь, что `C:\Program Files\nodejs\` есть в PATH
3. **Переустановите Node.js** с опцией "Add to PATH"

## 📝 Минимальные требования

- **Node.js**: версия 16 или выше
- **npm**: обычно устанавливается вместе с Node.js

