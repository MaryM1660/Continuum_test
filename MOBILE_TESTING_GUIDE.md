# Руководство по тестированию на реальных мобильных устройствах

## Проблема с эмуляцией в Desktop Chrome

Тестирование мобильной версии в Desktop Chrome с измененным User Agent **НЕ является** тестированием на реальном мобильном устройстве. На реальных мобильных устройствах:

1. **`enumerateDevices()` работает по-другому** - может возвращать пустые labels даже после `getUserMedia()`
2. **Разрешения обрабатываются иначе** - мобильные браузеры имеют дополнительные ограничения
3. **Системные ограничения** - iOS и Android имеют свои особенности работы с медиа-устройствами

## Способы тестирования на реальных мобильных устройствах

### 1. Remote Debugging (Chrome DevTools) - Рекомендуется

**Для Android:**

1. Подключите Android-устройство по USB
2. Включите "Отладка по USB" в настройках разработчика
3. На компьютере откройте Chrome и перейдите на `chrome://inspect`
4. Найдите ваше устройство и нажмите "Inspect"
5. Откройте ваш сайт на мобильном устройстве
6. Теперь вы можете видеть консоль, сетевые запросы и отлаживать код прямо на мобильном устройстве

**Для iOS (Safari):**

1. На iPhone/iPad: Настройки → Safari → Дополнительно → Веб-инспектор (включить)
2. Подключите устройство к Mac по USB
3. На Mac откройте Safari → Разработка → [Ваше устройство] → [Ваш сайт]
4. Откроется Web Inspector для мобильного Safari

### 2. Локальная сеть

Если ваш dev-сервер работает локально:

1. Узнайте IP-адрес вашего компьютера в локальной сети:
   - Windows: `ipconfig` в командной строке
   - Mac/Linux: `ifconfig` или `ip addr`
2. Убедитесь, что мобильное устройство подключено к той же Wi-Fi сети
3. Откройте на мобильном устройстве: `http://[IP-адрес]:3000` (или другой порт)
4. Например: `http://192.168.1.100:3000`

### 3. Ngrok/Tunneling

Для доступа к локальному серверу из интернета:

1. Установите ngrok: `npm install -g ngrok`
2. Запустите туннель: `ngrok http 3000`
3. Скопируйте URL (например: `https://abc123.ngrok.io`)
4. Откройте этот URL на мобильном устройстве

**Внимание:** Ngrok требует регистрации для HTTPS (необходимо для `getUserMedia()`)

### 4. Vercel Preview/Production

Если сайт уже задеплоен на Vercel:

1. Откройте URL на мобильном устройстве напрямую
2. Используйте Remote Debugging для отладки (см. выше)

## Что было исправлено для мобильных устройств

### 1. Использование track.label
- Если `enumerateDevices()` не возвращает labels, используем label из активного трека потока

### 2. Увеличенная задержка
- Для мобильных устройств задержка увеличена до 1 секунды перед `enumerateDevices()`

### 3. Повторные попытки
- На мобильных устройствах делается до 3 попыток получить список устройств

### 4. Улучшенное определение мобильных устройств
- Проверка по User Agent
- Проверка наличия touch events
- Проверка размера экрана

### 5. Поток не останавливается на мобильных
- На мобильных устройствах поток остается активным для правильного перечисления устройств

## Отладка на мобильных устройствах

### Проверка консоли

После подключения через Remote Debugging:

1. Откройте консоль в DevTools
2. Ищите логи с префиксами:
   - `🎤 [AUDIO]` - логи работы с аудио-устройствами
   - `📱 [AUDIO]` - специфичные для мобильных устройств
   - `✅ [AUDIO]` - успешные операции
   - `⚠️ [AUDIO]` - предупреждения
   - `❌ [AUDIO]` - ошибки

### Типичные проблемы на мобильных

1. **Только одно устройство "Default Microphone"**
   - Проверьте, что разрешение на микрофон было предоставлено
   - Проверьте, что поток активен (logs должны показывать `✅ [AUDIO] Permission granted`)
   - Убедитесь, что задержка достаточна (1 секунда для мобильных)

2. **Пустые labels**
   - Это нормально для некоторых мобильных браузеров
   - Код автоматически использует label из трека потока
   - Если это не помогло, устройство будет названо "Built-in Microphone"

3. **Голос не слышно**
   - Проверьте, что выбранное устройство используется в `getUserMedia()`
   - Проверьте логи в `voiceRecognition.ts` - там должен быть вызов `getUserMedia()` с правильным `deviceId`

## Тестирование выбора микрофона

1. Откройте приложение на мобильном устройстве
2. Пройдите онбординг
3. Нажмите на кнопку выбора микрофона (слева от основной кнопки микрофона)
4. Проверьте список устройств:
   - Должны отображаться все доступные микрофоны
   - Если подключены наушники/Bluetooth - они должны быть в списке
5. Выберите устройство
6. Проверьте, что выбранное устройство используется при записи

## Полезные команды для отладки

В консоли браузера (через Remote Debugging):

```javascript
// Проверить доступные устройства
navigator.mediaDevices.enumerateDevices().then(devices => {
  console.log('Devices:', devices.filter(d => d.kind === 'audioinput'));
});

// Проверить активный поток
console.log('Current stream:', window.currentMediaStream);

// Проверить разрешения
navigator.permissions.query({ name: 'microphone' }).then(result => {
  console.log('Microphone permission:', result.state);
});
```

## Следующие шаги

Если проблема сохраняется на реальном мобильном устройстве:

1. Откройте консоль через Remote Debugging
2. Скопируйте все логи с префиксом `[AUDIO]`
3. Проверьте, какие устройства возвращает `enumerateDevices()`
4. Проверьте, используется ли выбранное устройство в `getUserMedia()`


