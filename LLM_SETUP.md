# 🤖 Настройка LLM для голосового коуча

## Бесплатные варианты LLM API

### 1. Hugging Face Inference API (рекомендуется)

**Преимущества:**
- Полностью бесплатно для многих моделей
- Не требует регистрации для базового использования
- Множество моделей на выбор

**Как использовать:**
1. Модель уже настроена: `mistralai/Mistral-7B-Instruct-v0.2`
2. Для лучшей производительности можно получить бесплатный токен:
   - Зайдите на https://huggingface.co/settings/tokens
   - Создайте токен (Read access достаточно)
   - Добавьте в код (опционально, многие модели работают без токена)

**Альтернативные модели:**
- `meta-llama/Llama-2-7b-chat-hf` - более мощная, но требует токен
- `google/flan-t5-base` - быстрее, но меньше
- `microsoft/DialoGPT-medium` - специально для диалогов

### 2. Groq API (очень быстрый)

**Преимущества:**
- Очень быстрые ответы
- Бесплатный tier с хорошими лимитами
- Отличное качество

**Как использовать:**
1. Зарегистрируйтесь на https://console.groq.com
2. Получите API ключ
3. Обновите `llmService.ts` для использования Groq API

### 3. OpenAI API (платный, но есть бесплатный tier)

**Преимущества:**
- Лучшее качество ответов
- Очень надежный
- Есть бесплатный tier ($5 кредитов)

**Как использовать:**
1. Зарегистрируйтесь на https://platform.openai.com
2. Получите API ключ
3. Обновите `llmService.ts` для использования OpenAI API

## Текущая настройка

Приложение использует **Hugging Face Inference API** с моделью `Mistral-7B-Instruct-v0.2`.

### Системный промпт

LLM настроен как дружелюбный карьерный коуч:
- Помогает думать вслух о карьере
- Задает вдумчивые вопросы
- Поощряет честность
- НЕ дает готовых инструкций
- Фокусируется на размышлениях пользователя

### Как это работает

1. Пользователь говорит в микрофон
2. Web Speech Recognition API распознает речь
3. Текст отправляется в LLM
4. LLM генерирует ответ
5. Ответ озвучивается через Web Speech Synthesis API

## Настройка API ключа

Токен Hugging Face нужно добавить через переменные окружения для безопасности. См. `VERCEL_ENV_SETUP.md` для подробных инструкций.

### Локальная разработка:

1. Создайте файл `.env` в корне проекта (см. `.env.example`):
```
HUGGINGFACE_API_KEY=your_token_here
```

2. Токен будет автоматически использован из переменной окружения, если доступен.

### Vercel (Production):

1. Зайдите в настройки проекта на Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте переменную:
   - **Name:** `HUGGINGFACE_API_KEY`
   - **Value:** ваш токен
   - **Environment:** Production, Preview, Development (по необходимости)
4. Пересоберите проект

**Примечание:** Токен уже встроен в код для быстрого старта, но для production рекомендуется использовать переменные окружения для безопасности.

## Ограничения бесплатного tier

- **Hugging Face:** Модели могут быть "холодными" при первом запросе (нужно подождать 10-30 секунд)
- **Groq:** Ограничение на количество запросов в минуту
- **OpenAI:** Ограничение на количество токенов в месяц

## Рекомендации

Для прототипа и тестирования:
- ✅ Используйте текущую настройку (Hugging Face без токена)
- ✅ Если нужна скорость - попробуйте Groq
- ✅ Если нужна стабильность - используйте OpenAI с бесплатным tier

