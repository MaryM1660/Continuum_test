# 🚀 Быстрый деплой для демонстрации

## ⚡ Вариант 1: Vercel (рекомендуется - самый быстрый, 2-3 минуты!)

### Пошаговая инструкция:

1. **Зайдите на [vercel.com](https://vercel.com)** и войдите через GitHub (кнопка "Continue with GitHub")

2. **Нажмите "Add New Project"** (или "Import Project")

3. **Выберите репозиторий** `MaryM1660/Continuum_test` из списка

4. **Настройки проекта (Vercel определит их автоматически из vercel.json, но проверьте):**
   - Framework Preset: **Other** (или оставьте пустым)
   - Build Command: `npm run build:web` (уже настроено в vercel.json)
   - Output Directory: `web-build` (уже настроено в vercel.json)
   - Install Command: `npm install` (уже настроено в vercel.json)

5. **Нажмите "Deploy"**

✅ **Готово!** Через 2-3 минуты получите ссылку типа: 
- `https://continuum-test-xxx.vercel.app` (автоматическая)
- Можно настроить кастомный домен в настройках проекта

### 📱 Как показать друзьям:

1. Откройте ссылку Vercel на мобильном телефоне в браузере
2. Приложение автоматически адаптируется под мобильный экран
3. Отправьте ссылку друзьям - они увидят то же самое!

### 🔄 Автоматическое обновление:

Просто делайте `git push` в main - Vercel автоматически:
- Обнаружит изменения
- Пересоберет проект
- Обновит сайт (обычно за 1-2 минуты)

**Никаких дополнительных действий не требуется!** 🎉

---

## Вариант 2: Netlify (альтернатива)

1. Зайдите на [netlify.com](https://netlify.com) и войдите через GitHub
2. Нажмите "Add new site" → "Import an existing project"
3. Выберите репозиторий `MaryM1660/Continuum_test`
4. Настройки:
   - Build command: `npx expo export:web`
   - Publish directory: `web-build`
5. Нажмите "Deploy site"

---

## Вариант 3: Expo Go (для нативного опыта)

### Быстрая демонстрация через QR-код:

1. Запустите локально:
   ```bash
   npm start
   ```

2. Откройте Expo Go на телефоне:
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

3. Отсканируйте QR-код из терминала

⚠️ **Ограничение**: Работает только пока ваш компьютер включен и подключен к интернету

---

## Вариант 4: Expo EAS Build (для standalone приложения)

Для постоянного приложения без зависимости от Expo Go:

```bash
# Установите EAS CLI
npm install -g eas-cli

# Войдите в аккаунт Expo
eas login

# Настройте проект
eas build:configure

# Создайте build
eas build --platform ios
# или
eas build --platform android
```

⚠️ **Время**: 10-20 минут на сборку

---

## Рекомендация

**Для быстрой демонстрации друзьям:**
- ✅ **Vercel** - получите ссылку за 2-3 минуты, работает на всех устройствах
- ✅ Откройте ссылку в браузере мобильного устройства
- ✅ Приложение будет выглядеть как нативное благодаря responsive дизайну

**Для production:**
- Используйте Expo EAS Build для создания standalone приложений

